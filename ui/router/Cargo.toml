[package]
name = "telekinesis-router"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
description = "Shared provider catalog, key resolution, and local usage totals"

[dependencies]
rs_ai_providers = { path = "../../../rs_ai/crates/rs_ai_providers" }
dirs = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
