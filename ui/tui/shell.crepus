div w-full h-fit flex-col
  if {show_header}
    div h-fit flex-col
      span text-xs text-zinc-600
        "Telekinesis v{version} · press f1 for help"
      if {busy}
        span w-fit text-xs text-amber-400
          "{spinner}"

  div h-fit flex-col
    for message in {messages}
      if {block_break}
        div h-[1]
      if {is_tool}
        div h-fit flex-col
          span text-xs text-zinc-500
            "| {tool_name}"
          for line in {lines}
            span text-xs text-zinc-500
              "  {text}"
      else if {is_user}
        div h-fit flex-col
          for line in {lines}
            span text-sm text-blue-400
              "{text}"
      else
        div h-fit flex-col
          if {is_thinking}
            span text-sm text-zinc-500
              "· thinking"
          for line in {lines}
            span text-sm text-zinc-200
              "{text}"

  if {plan_prompt}
    div h-fit border-t border-indigo-700 flex-col
      span text-sm text-indigo-400
        "| plan approval"
      for line in {plan_rows}
        span text-xs text-zinc-400
          "  {text}"
      span text-xs text-zinc-500
        "  y approve · n reject"
  else if {permission_prompt}
    div h-fit border-t border-amber-700 flex-col
      span text-sm text-amber-400
        "| permission · {permission_tool}"
      span text-xs text-zinc-500
        "  y allow · n deny"

  if {has_active_plan}
    div h-fit flex-col
      span text-xs text-indigo-400
        "| plan (auto-approved)"
      for line in {active_plan_rows}
        span text-xs text-zinc-400
          "  {text}"

  div h-fit flex-col
    if {provider_menu_open}
      div h-fit flex-col
        span text-sm text-indigo-400
          "providers · type to search · enter details · esc close"
        for provider in {provider_rows}
          if {selected}
            span text-sm text-indigo-300
              "  › {name} · {id}"
            span text-xs text-indigo-500
              "    {env}"
          else
            span text-xs text-zinc-500
              "    {name} · {id} · {env}"
          if {configured}
            span text-xs text-green-400
              "    configured"
    else if {login_menu_open}
      div h-fit flex-col
        span text-sm text-indigo-400
          "OAuth login · select a provider"
        for oauth in {login_rows}
          if {selected}
            span text-sm text-indigo-300
              "  › {display} ({id})"
          else
            span text-xs text-zinc-500
              "    {display} ({id})"
          if {configured}
            span text-xs text-green-400
              "    configured"
        span text-xs text-zinc-600
          "↑/↓ select · enter start browser login · esc cancel"
    else if {apikey_detail_open}
      div h-fit flex-col
        span text-sm text-indigo-400
          "{apikey_name} ({apikey_id})"
        span text-xs text-zinc-500
          "  status: "
          if {apikey_configured}
            span text-xs text-green-400
              "configured"
          else
            span text-xs text-amber-400
              "not configured"
        if {apikey_has_keychain}
          span text-xs text-green-400
            "  keychain: saved"
        span text-xs text-zinc-500
          "  API key env: {apikey_env}"
        span text-xs text-zinc-500
          "  endpoint: {apikey_url}"
        span text-xs text-zinc-500
          "  default model: {apikey_default_model}"
        span text-xs text-zinc-500
          "  models: {apikey_models}"
        if {apikey_input_open}
          div h-[1]
          span text-xs text-indigo-400
            "Paste API key, then press Enter:"
          span text-sm text-indigo-300
            "  {apikey_edit_buffer}▌"
          span text-xs text-zinc-600
            "esc cancel"
        else if {apikey_status}
          div h-[1]
          span text-xs text-green-400
            "{apikey_status}"
          span text-xs text-zinc-600
            "enter back · esc close"
        else
          div h-[1]
          for row in {apikey_action_rows}
            if {selected}
              span text-sm text-indigo-300
                "  › {action}"
            else
              span text-xs text-zinc-500
                "    {action}"
          span text-xs text-zinc-600
            "↑/↓ select · enter run · esc close"
    else if {config_open}
      div h-fit flex-col
        span text-sm text-indigo-400
          "config · ↑/↓ select · enter apply · esc close"
        for row in {config_rows}
          if {selected}
            span text-sm text-indigo-300
              "  › {label}"
            span text-xs text-indigo-500
              "    {hint}"
          else
            span text-xs text-zinc-500
              "    {label}"
            span text-xs text-zinc-600
              "    {hint}"
    else if {selecting_model}
      div h-fit flex-col
        span text-sm text-indigo-400
          "model · ‹ {selected_provider} › · search: {input_before}{cursor}{input_after}"
        for model in {model_rows}
          if {selected}
            span text-sm text-indigo-300
              "  › {model_id}"
          else
            span text-xs text-zinc-500
              "    {model_id}"
        if {no_model_matches}
          span text-xs text-amber-400
            "  no models match — check the provider or /login to add one"
        span text-xs text-zinc-600
          "←/→ provider · ↑/↓ model · type to search all · enter apply · esc cancel"
    else
      if {has_slash_suggestions}
        div h-fit flex-col
          for command in {slash_rows}
            if {selected}
              span text-sm text-indigo-300
                "  › {command} — {desc}"
            else
              span text-xs text-zinc-500
                "    {command} — {desc}"
          span text-xs text-zinc-600
            "↑/↓ command · tab insert"
      else if {has_file_suggestions}
        div h-fit flex-col
          for file in {file_rows}
            if {selected}
              span text-sm text-indigo-300
                "  › @{path}"
            else
              span text-xs text-zinc-500
                "    @{path}"
          span text-xs text-zinc-600
            "↑/↓ file · tab insert"
      span text-sm text-{prompt_color}
        "{prompt_char} {input_before}{cursor}{input_after}"

  div h-fit flex-col
    div h-[1] flex-row gap-2
      span w-fit text-xs text-zinc-600
        "${cost}"
      span w-fit text-xs text-zinc-600
        "· {usage}"
      span w-fit text-xs text-zinc-600
        "· {project} ({branch})"
    div h-[1] flex-row gap-2
      span w-fit text-xs text-indigo-400
        "{model} · {effort} · {agent_mode}"
      span w-fit text-xs text-{context_color}
        "{context_pct}%/{context_window}"
      span w-fit text-xs text-zinc-600
        "· {permission_mode}"
      if {providers_connecting}
        span w-fit text-xs text-amber-400
          "· connecting providers…"
    if {has_running_subagents}
      for subagent in {running_subagents}
        span text-xs text-zinc-500
          "↳ {name}"
